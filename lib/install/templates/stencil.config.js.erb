const sass = require('@stencil/sass');

exports.config = {
  namespace: '<%= Rails.application.class.parent_name.downcase %>',
  srcDir: 'app/javascript/components/',
  wwwDir: 'public/components/',
  publicPath: '/components/build',
  bundles: [
    { components: ['hello-world'] }
  ],
  plugins: [
    sass()
  ]
};

exports.devServer = {
  root: 'public/components',
  watchGlob: '**/**'
}
